# Metadados da API
APP_NAME="Pipeline Jurídico NLP"
APP_VERSION=0.1.0

# Servidor (usado por `python main.py`)
HOST=127.0.0.1
PORT=8000
RELOAD=true

# Chunking — valores padrão, sobrescrevíveis por requisição
CHUNK_SIZE=350
CHUNK_OVERLAP=80

# Anonimização (LGPD / Resolução CNJ nº 615/2025)
# Idioma usado no AnalyzerEngine do Presidio (correção do bug: deve ser "pt", não "en")
PRESIDIO_LANGUAGE=pt
# Modelo spaCy a ser baixado com: python -m spacy download pt_core_news_lg
SPACY_MODEL=pt_core_news_lg

# Síntese (mock. ver app/summarization/service.py para o ponto de extensão)
SUMMARIZER_MODEL_NAME="Meta-Llama-3-70B-Instruct-Legal"
